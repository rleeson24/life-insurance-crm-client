# Standalone API (dotnet run on the BrokerBook API project):
VITE_API_BASE_URL=https://localhost:7114

# When running via Aspire AppHost, VITE_API_BASE_URL is injected automatically.

# Entra SPA registration (BrokerBookCRM-SPA). Same values for local, Azure dev, and prod.
# Redirect URI registered in Entra must match the origin (local: http://localhost:5387/).
VITE_AZURE_AD_CLIENT_ID=
VITE_AZURE_AD_TENANT_ID=
VITE_AZURE_AD_API_SCOPE=api://6c970234-fee3-4568-97d8-7d015c903368/access_as_user
